# Juegos descargados (privados y con derechos de autor de terceros).
# NUNCA subir al repositorio público.
games/
*.zip
*.rar
*.7z
*.tgz

# Capturas personales para documentación (no van al repo público)
FOTOS-GITHUB/

# Binarios / artefactos de build
runtimes/mkxp-z
runtimes/RPGMakerDecrypter-cli
runtimes/RPGMakerDecrypter-cli.exe
__pycache__/
*.pyc

# Logs y temporales
*.log
.DS_Store
Thumbs.db

# Configuración local del usuario (atajos de teclado, preferencias)
launcher-config.json
launcher-state.json

# Paquetes y staging de build
dist/
packaging/.flatpak-src/
packaging/flatpak-build/
packaging/flatpak-repo/
.flatpak-builder/
# Backups de partidas (datos de usuario)
backups/
zooms/
