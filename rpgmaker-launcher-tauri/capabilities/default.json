{
  "identifier": "default",
  "description": "Permisos base de la ventana principal: núcleo + diálogos nativos (selector de carpetas).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "shell:default"
  ]
}
